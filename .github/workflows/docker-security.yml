name: Docker Security Scan

on:
  schedule:
    - cron: "0 9 * * 1"
  pull_request:
    paths:
      - "docker-compose.yml"
      - "apps/api/Dockerfile"
      - "apps/web/Dockerfile"
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write
  security-events: write

jobs:
  scout-scan:
    name: Scout CVE scan (${{ matrix.image }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        image:
          - postgres:16-alpine
          - redis:7-alpine
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Compute safe image slug
        id: slug
        run: |
          slug=$(echo "${{ matrix.image }}" | tr ':/' '__')
          echo "value=$slug" >> "$GITHUB_OUTPUT"

      - name: Docker Hub login
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Docker Scout - scan + SARIF + PR summary
        uses: docker/scout-action@v1
        with:
          command: cves
          image: ${{ matrix.image }}
          sarif-file: scout-${{ steps.slug.outputs.value }}.sarif
          only-severities: critical,high
          write-comment: ${{ github.event_name == 'pull_request' }}
          summary: true
          github-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Sanitize SARIF (clamp region fields to >= 1)
        run: |
          f="scout-${{ steps.slug.outputs.value }}.sarif"
          jq 'walk(
                if type == "object"
                then with_entries(
                  if (.key | IN("startLine","startColumn","endLine","endColumn"))
                     and (.value | type == "number")
                  then .value = ([.value, 1] | max)
                  else . end)
                else . end)' "$f" > "$f.tmp" && mv "$f.tmp" "$f"

      - name: Upload SARIF to GitHub Security tab
        if: always()
        continue-on-error: true
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: scout-${{ steps.slug.outputs.value }}.sarif
          category: docker-scout-${{ steps.slug.outputs.value }}

  scout-compare:
    name: Scout diff-only gate (${{ matrix.image }})
    if: github.event_name == 'pull_request'
    needs: scout-scan
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        image:
          - postgres:16-alpine
          - redis:7-alpine
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Docker Hub login
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Compare PR image against main baseline
        uses: docker/scout-action@v1
        with:
          command: compare
          image: ${{ matrix.image }}
          to: ${{ matrix.image }}
          to-ref: main
          ignore-unchanged: true
          only-severities: critical,high
          exit-code: true
          exit-on: vulnerability
          github-token: ${{ secrets.GITHUB_TOKEN }}
