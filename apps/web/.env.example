# Frontend env (Next.js client). NEXT_PUBLIC_ vars are exposed in the
# browser bundle — do not put secrets here.

# Backend API base URL. Used by lib/api.ts > API_BASE and by every fetch
# in the frontend (login, refresh, /me, all the data hooks).
NEXT_PUBLIC_API_URL=http://localhost:8000
